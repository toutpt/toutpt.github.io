baseURL = "http://example.org/"
languageCode = "en-us"
title = "My Blog"
theme = "mainroad"

[Author] # Used in authorbox
  name = "Jean-Michel FRANCOIS"
  bio = "Open Source pationate dev to build usefull tools"
  avatar = "images/avataaars.png"


[Params]
  subtitle = "Just another blog" # Subtitle of your site. Used in site header
  description = "Jean-Michel's Personal blog about everything" # Site description. Used in meta description
  copyright = "Jean-Michel FRANCOIS" # copyright holder, otherwise will use site title
  opengraph = true # Enable OpenGraph if true
  twitter_cards = true # Enable Twitter Cards if true
  readmore = false # Show "Read more" button in list if true
  authorbox = true # Show authorbox at bottom of pages if true
  toc = true # Enable Table of Contents
  post_navigation = true # Show post navigation at bottom of pages if true
  post_meta = ["date", "categories", "translations"] # Order of post meta information
  #mainSections = ["post", "blog", "news"] # specify section pages to show on home page and the "Recent articles" widget
  #dateformat = "2006-01-02" # change the format of dates
  #mathjax = true # Enable MathJax
  #mathjaxPath = "https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.5/MathJax.js" # Specify MathJax path
  #mathjaxConfig = "TeX-AMS-MML_HTMLorMML" # Specify MathJax config
  #highlightColor = "#e22d30" # Override highlight color
  #customCSS = ["css/custom.css"] # Include custom CSS files
  #customJS = ["js/custom.js"] # Include custom JS files

[Params.sidebar]
  home = "right" # Configure layout for home page
  list = "left"  # Configure layout for list pages
  single = false # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["search", "recent", "categories", "taglist", "social", "languages"]


[Params.widgets.social]
  # Enable parts of social widget
  # facebook = "username"
  twitter = "toutpt_fr"
  github = "toutpt"
  email = "toutpt@gmail.com"